# Decision-Trees-for-Classification
A Decision Tree Classifier was trained on the Iris dataset to predict flower species. The tree structure was visualized using matplotlib. Pruning was applied using max_depth=3 to reduce overfitting. The model was evaluated using Accuracy and F1-Score, achieving high classification performance. 
